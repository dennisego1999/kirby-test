{
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "npm run createBuildDir && vite build --emptyOutDir --base=/build",
		"typescript": "tsc --noEmit",
		"createBuildDir": "mkdir -p public"
	},
	"dependencies": {
		"laravel-vite-plugin": "^1.2.0",
		"modern-normalize": "^3.0.1",
		"sass": "^1.83.4",
		"vite": "^6.0.11",
		"vite-plugin-sass-glob-import": "^5.0.0"
	},
	"devDependencies": {
		"typescript": "^5.7.3",
		"vite-plugin-checker": "^0.8.0"
	}
}
